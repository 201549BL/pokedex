import React from "react";
import styled from "styled-components";

const StyledInfoPage = styled.div``;

const InfoPage = ({ children }) => {
  return <StyledInfoPage>{children}</StyledInfoPage>;
};

export default InfoPage;
